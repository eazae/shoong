import { getScreenHeight } from '@utils/native';
import styled from 'styled-components/native';

const SCREEN_HEIGHT = getScreenHeight();

export const AuthLayOut = styled.View`
  margin: 12px 0px;
  justify-content: center;
`;
